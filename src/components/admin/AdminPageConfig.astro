---
interface Props {
  config: Record<string, unknown>;
}

const { config } = Astro.props;
const json = JSON.stringify(config).replace(/</g, '\\u003c');
---

<script type="application/json" id="adm-page-config" set:html={json}></script>
